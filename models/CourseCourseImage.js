
const mongoose = require('mongoose');
const Schema = mongoose.Schema;
const CourseCourseImageSchema = new Schema({
data:String , 
name:String 
});

module.exports = { CourseCourseImageSchema };

